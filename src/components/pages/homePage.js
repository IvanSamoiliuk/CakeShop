import React from "react";
import CakeList from "../cakeList/cakeList";

const HomePage = () => {
    return (
        <div>
            <CakeList />
        </div>
    );
};

export default HomePage;
