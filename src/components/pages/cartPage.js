import React from "react";
import OrderContainer from "../order";

const CartPage = () => {
    return (
        <div>
            <OrderContainer />
        </div>
    );
};

export default CartPage;
