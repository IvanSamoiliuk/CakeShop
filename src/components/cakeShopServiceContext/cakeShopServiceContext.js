import React from "react";

const {
    Provider: CakeShopServiceProvider,
    Consumer: CakeShopServiceConsumer,
} = React.createContext();

export { CakeShopServiceProvider, CakeShopServiceConsumer };
